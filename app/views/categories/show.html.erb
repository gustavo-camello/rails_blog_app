<h1> <%= @category.name %> </h1>

<% if logged_in? && current_user.admin? %>
  <div>
    <%= link_to "Edit the category", edit_category_path(@category) %>
  </div>
<% end %>

<h3> Articles </h3>

<%= render 'articles/article' %>
